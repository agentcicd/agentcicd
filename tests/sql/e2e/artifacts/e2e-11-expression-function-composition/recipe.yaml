id: e2e-11-expression-function-composition
name: Expression and function composition
description: Unary, binary, boolean, SQL/local/runtime function, JSON, helper, CTE, and concat composition.
markers: [spark, e2e]
include_cells: true
inputs:
  expression_inputs_csv: inputs/expression_inputs.csv
fixtures:
  - fixtures/normalize_text.yaml
  - fixtures/embed.text.yaml
execution:
  kind: recipe
expected:
  tables: expected/tables.yaml
  schemas: expected/schemas.yaml
  manifests: expected/manifests.yaml
  fixture_calls: expected/fixture_calls.yaml
